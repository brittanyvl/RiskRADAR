# Scraping
selenium>=4.0.0
webdriver-manager>=4.0.0

# PDF processing (Phase 3)
pymupdf>=1.23.0  # Faster and more powerful than pypdf

# OCR (Phase 3)
pytesseract>=0.3.10
pdf2image>=1.16.0
Pillow>=10.0.0
pandas>=2.0.0  # Required for OCR confidence analysis

# Chunking (Phase 4)
tiktoken>=0.5.0  # Token counting for chunk size management

# Analytics (DuckDB)
duckdb>=0.9.0  # SQL analytics engine with Parquet support

# Database (using stdlib sqlite3 - no additional deps needed)

# Vector DB (Phase 5)
qdrant-client>=1.7.0

# Embeddings (Phase 5)
sentence-transformers>=2.2.0
pyarrow>=14.0.0  # Parquet storage for embeddings

# App (Phase 8)
# streamlit>=1.30.0

# Development
python-dotenv>=1.0.0
