# RiskRADAR Environment Variables - TEMPLATE
# Copy this file to .env and fill in your values
# The .env file is gitignored and will not be committed

# ============================================================================
# Core Paths (Optional - defaults are usually fine)
# ============================================================================
# PDF storage on NAS (default: \\TRUENAS\Photos\RiskRADAR)
# RISKRADAR_NAS_PATH=\\TRUENAS\Photos\RiskRADAR

# SQLite database path (default: sqlite/riskradar.db relative to project root)
# RISKRADAR_DB_PATH=sqlite/riskradar.db

# Log directory (default: logs/ relative to project root)
# RISKRADAR_LOG_DIR=logs

# ============================================================================
# Phase 1: Scraper Configuration (Optional - defaults are sensible)
# ============================================================================
# Run browser in headless mode (default: 1, set to 0 for debugging)
# SCRAPER_HEADLESS=1

# Temporary downloads directory (default: .scraper_tmp_downloads)
# SCRAPER_DOWNLOADS_DIR=.scraper_tmp_downloads

# Download timeout in seconds (default: 120)
# SCRAPER_DOWNLOAD_TIMEOUT=120

# Browser user agent (default: RiskRADARBot/1.0)
# HTTP_USER_AGENT=RiskRADARBot/1.0

# Rate limiting delays (seconds) - DO NOT reduce below defaults!
# SCRAPER_REQUEST_DELAY=2.0
# SCRAPER_DOWNLOAD_DELAY=3.0
# SCRAPER_PAGE_DELAY=2.0

# ============================================================================
# Phase 5: Qdrant Cloud Configuration (Required for embeddings)
# ============================================================================
# 1. Create account at https://cloud.qdrant.io/
# 2. Create a cluster (free tier is sufficient)
# 3. Go to API Keys tab and create a new key
# 4. Copy the cluster URL and API key below

QDRANT_URL=https://your-cluster-id.region.aws.cloud.qdrant.io:6333
QDRANT_API_KEY=your_api_key_here
